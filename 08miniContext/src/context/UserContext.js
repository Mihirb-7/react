import React from 'react'

//we can simply say context is a global variable
const UserContext = React.createContext()   //creating context variable to store context

export default UserContext
//every context is a provider


